# Bay Ave Barnacle — Flood Prediction Model v0.10.6

**Promotion date: 2026-09-24.** Candidate `1053eb436` independently
reviewed by Codex in audit rounds01/03/05, with Claude's replies02/04.
Final approval: [round05](../audits/2026-09-24-a1/05-candidate-approval-codex.md).
John's pre-promotion approval is recorded in BACKLOG DECISION
`v0.10.6-promotion`. Rule-5 class (a) + (b): a FORMULA change (surge decay)
with new replay goldens, plus inputs and policy. Frozen v0.10.1/v0.10.3
replays pass unchanged; the new v0.10.6 replay passes.
Superseded specification: [`archive/v0.10.5.md`](archive/v0.10.5.md).

Honest cutover: the promoting merge regenerates the site on integrated main
at `2026-09-24T05:46:59Z`. Candidate-branch pages already carried v0.10.6,
but were review previews, not production deployment. Generated per-tide
`evolution.csv` files are rebuilt from the canonical predictions ledger;
preview-only rows do not become historical production predictions. Existing
production ledger rows retain their as-run stamps, including v0.10.5.
The new run appends predictions, day-risk and outlook rows and the first
prospective replay-input record, with notification sending disabled. The
first deployed generation is verified in the audit close-out; generation,
commit and deployment times are distinct.

## What changed from v0.10.5

1. **Surge decays toward the recent average** (owner decisions 2026-09-23:
   BACKLOG `fresh-surge-decay`, `missing-surge-ladder`,
   `missing-surge-ladder-decay`). v0.10.5 held the observed surge constant
   for every future hour; v0.10.6 lets it decay, from the reading's own time,
   toward the trailing 364-day mean surge with a 36-hour time constant. The
   same formula covers a fresh reading and a silent gauge.
2. **Missing-reading ladder** replaces v0.10.5's interim "no curve" rule:
   fresh -> stale-download -> stale-state -> typical offset, each labeled.
3. **Typical offset instead of zero** wherever no reading exists (per-tide
   and curve): Sandy Hook's mean surge is about +0.5 ft (sea level above the
   1983-2001 tidal epoch); zero is biased low.
4. **One hourly surge estimator for the 7-day page** (chart line, table,
   extended maps), P-ETSS used hourly, a guidance-to-decay tail, labeled
   source boundaries and advisory markers (seven-day review, below).
5. **New production inputs:** the trailing mean (`surge_mean`, warm job,
   with a fallback constant) and the surge state file (`surge_state`).

### Evidence (history, not live skill)
Sandy Hook hourly 2006-2026-05, forecasting the surge k hours after the
reading; MAE ft, constant -> decay toward the trailing 365-d mean, tau 36 h
(`history/scripts/surge_decay_views.py`, `history/reports/2026-09-23-surge-decay-views.txt`):

| View | 12 h | 24 h | 30 h |
|---|---|---|---|
| All hours (curve, tank) | 0.296 -> 0.273 | 0.399 -> 0.343 | 0.449 -> 0.369 |
| High tides (per-tide peaks) | 0.291 -> 0.267 | 0.394 -> 0.335 | 0.453 -> ~0.36 |
| Low tides (added for review 2026-09-24-a1 R6) | 0.291 -> 0.268 | 0.404 -> 0.344 | 0.441 -> 0.370 |
| Bay near the plug band (2.5-3.8 ft NAVD88) | 0.283 -> 0.273 | 0.385 -> 0.356 | 0.446 -> 0.390 |
| Storm starts (reading >= +1 ft) | 0.502 -> 0.436 | 0.739 -> 0.559 | 0.827 -> 0.574 |

tau 24-48 h are close; 36 h is best or near-best in every view and in both
halves of the record. Decaying toward ZERO loses to constant near the plug
band; toward the mean it wins there too. Research correction (2026-09-24):
UTC-aligned past wind/pressure modestly improves storm errors but fails the
multi-view criterion near the plug band. Forecast wind/pressure remains a
promising exploratory candidate, not established live skill. The UTC decay
rerun supports retaining tau36. See the [wind audit close-out](../audits/2026-09-24-a2/03-close-out-codex.md)
and [approved shadow plan](../history/plans/2026-09-24-wind-term-candidate-plan.md).
This evidence clarification changes no production formula or coefficient.

### Live comparison on one issuance (2026-09-24 ~04:02Z, v0.10.5 vs candidate)
Same live inputs, no-send runs a few seconds apart
(`history/reports/2026-09-24-v0.10.6-live-comparison.txt`; checklist items 2-3 numbers in `history/reports/2026-09-24-v0.10.6-advisory-and-petss.txt`):
- 30-h curve: identical through the reading, then lower by up to 0.93 ft at
  +30 h (reading +2.17 ft decaying toward +0.53 ft). Tank unchanged because
  NO RAIN was forecast in that window (the curve itself exceeded 4 ft NAVD88;
  this dry run does not test tank sensitivity; see item 5 for wet cases). Per-tide values unchanged (all NWS
  advisory rows). Alert level unchanged (moderate, Fri 19:40 tide).
- Today's headline moderate -> light and the landing TODAY regime moderate ->
  street: v0.10.5's constant curve sat 0.40-0.59 ft ABOVE the advisory at the
  advisory's own tides and set the day's worst; the candidate's curve sits
  0.31 ft above (Thu 06:47) and 0.29 ft below (Thu 19:01) the advisory, so the
  day's worst falls back to the advisory tide (7.0 ft, light).
- 7-day line: identical where the advisory/NWPS applies; P-ETSS hourly mean
  +0.10 ft vs the old per-tide interpolation; beyond 102 h +0.62 ft mean, of
  which ~+0.5 ft is the typical offset v0.10.5 omitted. Cards Sep 28-30 move
  from dry to light/street/street; the worst-flood-chance selection is
  unchanged.

## Seven-day review checklist: disposition
(`history/plans/2026-09-23-v0.10.6-outlook-review.md`, John 2026-09-23 23:55)

1. **Source and assumption boundaries — DONE.** Every hour carries its
   source; the 7-day chart shades each contiguous source run, draws a dashed
   boundary at every switch and names the source at the top; red diamonds
   show the advisory's own high-tide numbers (also on the landing chart).
   Visually verified in headless Chrome on the live candidate run.
2. **NWS advisory corrections — RETAINED AS EXPERIMENTAL, PROSPECTIVELY
   LOGGED (owner decision 2026-09-24, adopting Codex's recommendation in
   audits/2026-09-24-a1).** One live capture (2026-09-24 03:57Z): advisory
   minus the NWS gauge forecast at five advisory tides = +0.0, +0.1, +0.1,
   +0.4, +0.4 ft; applied to the hourly line it averaged +0.21 ft at
   high-tide hours, +0.19 mid, +0.12 low (max +0.40). A later capture
   (04:10Z) had six ZERO corrections: the 0-0.4 ft range is one capture's
   range, not a bound, and 0.4 ft (4.8 in) can matter near a street or drain
   threshold. Not called negligible or validated. Every run now archives the
   raw hourly NWPS (issuance and retrieval times), the advisory rows, the
   hourly astronomy and the corrected hourly output with its per-hour source
   and model version (`data/replay_inputs/`, `forecast/replay_archive.py`),
   so raw-vs-corrected errors at high, mid and low tide can be scored against
   later gauge observations.
3. **Hourly P-ETSS — DONE.** P-ETSS midpoint is used hourly for hours NWPS
   does not cover, to 102 h (replacing interpolation between per-tide
   values). Freshness/coverage via the existing admission (ok <= 12 h,
   degraded <= 30 h). Bias: P-ETSS ran up to +0.47 ft above NWPS late in the
   shared window; one scored tide (+0.21 ft high) is not enough to correct,
   so no offset is applied and its bias is stated as unmeasured.
4. **Transition into mean-reverting decay — DONE.** The tail decays from the
   LAST guidance value (continuous at the boundary by construction: 1.35 ->
   1.33 ft live), toward the typical offset, labeled as an assumption and
   drawn with triangles on the per-tide chart. Every fallback rung of the
   table (guidance decay, observed-reading decay from the READING's time,
   typical offset) comes from the same estimator as the hourly line (review
   R1); expiring or missing sources move the boundary visibly (tests: no
   NWPS, no P-ETSS, neither; fresh, stale-download, stale-state, no reading).
5. **Candidate vs baseline, incl. rain — BOUNDED COMPARISON DONE (r2);
   residual deferred by the owner (decision 2026-09-24, Codex's
   recommendation).** Method predeclared in the review checklist; script
   `history/scripts/v0106_rain_comparison.py` (r2), output
   `history/reports/2026-09-24-v0.10.6-rain-comparison-r2.txt` (supersedes
   r1, which is kept with an appended pointer).
   - Surge, history: all hours, high tides, LOW tides, plug band, storm
     starts: decay better at every lead 6-48 h (table above). Surge errors,
     not street-depth skill.
   - Controlled wet scenarios (identical rain; sensitivity, not skill),
     grouped by the maximum v0.10.5 bay over the simulated window: never
     reaching 3.0 ft NAVD88 -> no change (30 of 30); reaching the plug band
     -> at most -0.2 in (12 cases, mean -0.03); above 3.52 ft -> the
     candidate's lower bay lowers the rain peak by 0 to 2.0 in (12 cases,
     mean -0.72) and can shorten time above the curb (140 -> 60 min in one
     case). Grouping by the bay at the burst CENTER instead puts a -2.0 in
     case in "below 3.0" (it crosses the band later); both tables are in the
     report.
   - Observed rain floods with MRMS rain (reconstructed hindcast, NOT
     as-issued skill; issuance reading = the last hourly reading at or
     before issuance, decaying from its own time). References are labeled by
     evidence type, and none is a tape-measured crest in every case:
     07-06 tape series, accepted window +15.0..+15.8 (canonical +15.4);
     08-03 live-narrated two-landmark bracket +13.8; 08-07 recession backcast
     +15.0..+15.8; 09-01 photo landmark bracket +13.7..+14.2; 09-13
     photo-verified lawn-step level +13.7. On these five events x 3 leads the
     two rules TIE: bay error at rain onset 0.43 vs 0.43 ft; rain-peak error
     1.42 vs 1.42 in (canonical) and 1.20 vs 1.20 in (brackets); the
     simulated peaks are identical in all 15 cases (the bay stayed below the
     drain band). This sample cannot distinguish the rules.
   - 2025-10-30 (compound; reference RECONSTRUCTED, no tape measurement,
     only the photo's +13.5 in is a bound): kept out of the aggregates. The
     tide gauge shows the decay under-predicting the building bay by about
     0.3 ft more than the constant rule (rain-onset bay 3.69 vs 3.98 vs
     observed 4.13 ft at 6 h); simulated peaks +15.4 vs +15.7 in; which rule
     was closer to the unknown crest is undetermined.
   - EXPLORATORY history check (not predeclared): with the surge RISING at
     the reading (n ~ 13,900) decay still beats constant (24 h: 0.540 vs
     0.656 ft).
   - Sep 13, PARTIAL-INPUT SENSITIVITY EXPERIMENT (not forecast skill): the
     2026-09-13T03:14:36Z forecast saved hourly QPF only for its tides'
     windows; the 09:58 tide's window covers 11-17Z and does not include the
     10Z hour of the first burst, which is zero here BY ASSUMPTION. Maxima of
     the tank over the constructed window: observed bay +8.0 in; the
     archived v0.10.3 curve (the one genuine archived output) +5.7 in;
     counterfactual constant (archived reading +0.487 ft) +4.8 in; decay
     +5.2 in; reference +13.7 in. No forecast-error attribution is made.
   - Residual (deferred, owner): multi-event AS-ISSUED rain-flood skill of
     the candidate. Needs issuance-time rain, surge state and tank
     initialization, which `data/replay_inputs/` now collects every run.
6. **Surfaces (AGENTS rule 8) — enumerated below; numerical consistency
   checked** where surfaces claim the same quantity (7-day line vs table:
   same estimator; production curve vs outlook with no guidance: identical,
   tested). The production curve and the NWS advisory deliberately differ
   and are shown side by side. Missing/stale inputs and source boundaries are
   exercised in tests. Visual check of rendered charts: done locally; the
   DEPLOYED charts are for the independent reviewer after merge.
7. **Spec, goldens, review, decision — spec (this file) and NEW replay golden
   (`model/data/v0.10.6-reproduction.json`, `history/scripts/reproduce_v0_10_6.py`)
   done; independent review and John's DECISION outstanding.**

## Surfaces and arms (AGENTS rule 8)
| Arm | What changes | Treatment |
|---|---|---|
| Landing chart | curve decays; advisory diamonds; label names reading age, mean, tau | regenerated |
| Widget (Scriptable) | its curve reads `water_series` (decays automatically); degraded line may show the raw key `surge_mean` | OBJECTIVE EXEMPTION from a code edit: owner decision "widget unchanged"; the key reads plainly; no re-copy needed |
| Landing strip / cards / ribbon, email subject/text/HTML | `day_worst` from the decayed curve + unchanged per-tide values | recomputed by the same function |
| Landing / details / per-tide maps | first 30 h decayed curve; later points the outlook estimator; seam labeled | regenerated |
| Town map | reads the same payloads | no code change needed (reads `water_series`/outlook) |
| 7-day page | estimator, source bands, diamonds, cards/table | regenerated |
| ntfy/email alert gate | per-tide values: product rows unchanged; persistence tides now decay (lower in storms at 24-48 h) | intended; 48-h window unchanged |
| SMS / radar nowcast | unchanged (explicit 0.0 caller; own bay fallback) | exempt: does not carry the concept |
| predictions_log | `surge_ft_predicted` is the decayed value at each tide; new `typical-offset-degraded` label | append-only; gate accepts the label |
| outlook_log | `persist_decay_mllw` now toward the mean with tau 36 h (was toward zero, 48 h); `outlook_source` may be `guidance_decay` | rows carry `model_version`; score by version |
| day_risk_log | day_worst inputs changed | append-only |
| replay_inputs (new) | per-run raw inputs for items 2 and 5 | append-only, gate-checked |
| shadow scoring | changed-formula columns scored only on rows from v0.10.6 on (`FORMULA_SINCE`), reported as cohorts | review R3 |

## Prospective replay-input archive (v0.10.6)
`data/replay_inputs/YYYY-MM.jsonl`, one line per hourly run, append-only,
gate-checked (`forecast/replay_archive.py`): raw hourly NWPS with issuance and
retrieval times, NWS advisory rows and corrections, hourly astronomy and the
corrected outlook surge with per-hour sources (-6..72 h), production hourly
QPF as issued, the surge ladder rung/reading/mean used, tank initialization,
model version; unavailable inputs recorded as unavailable. Hourly arrays are
columnar (start + step). About 4 KB per run. A failed write shows as the
outlook-scoped health entry `outlook_replay_archive` and never breaks a run.

## Known limitations
- On five observed rain floods (radar rain) the two rules tie exactly; on
  the reconstructed 2025-10-30 compound storm the decay under-predicted a
  building bay by ~0.3 ft more (gauge) and its effect on that storm's
  unmeasured crest is undetermined. History over ~14,000 rising-surge
  readings favors decay. Multi-event as-issued rain-flood skill is unmeasured.
- The decay law is measured on observed readings; applying it to the last
  guidance value (7-day tail) is an assumption.
- One average tau for every weather situation; storm persistence is not
  modeled (forecast wind is the next version's candidate input).
- The production curve ignores the NWS advisory between/at its tides by
  design; the advisory drives per-tide values, alerts and the day's worst
  when it is higher. The gap is displayed.
- The trailing mean lags ~3 weeks (verified data).

## Inputs & policy

### Sources, units, validity (all with input_health entries)

| Source | Product | Units | Reach | Refresh | Validity / admission |
|---|---|---|---|---|---|
| astro, astro_hourly | NOAA CO-OPS predictions, Sandy Hook 8531680 | ft MLLW | any | hourly run | finite values only |
| grid | NWS gridpoint (PHI 87,105, the cell the points endpoint returns for the house; corrected 2026-09-24, previously mislabeled PHI 61,102): QPF, PoP, wind, gust, direction | in, %, mph, deg | QPF ~72 h; wind/PoP ~7 d | hourly run | ISO durations parsed; day-total aggregation assumes absent QPF intervals inside reach are dry; explicit nulls reduce coverage |
| nwps | NWS water-prediction gauge forecast SDHN4 | ft MLLW hourly | 72 h | hourly run | issuance parseable, not future, ≤ 24 h old; units ft; finite, −5..25; ≥ 12 future points. **SHADOW**: never the production surge source |
| nws_product | KPHI CFW raw product, Sandy Hook table | ft MLLW per high tide | ~3 d, events only | hourly run | product issued at the alert's `sent`, else newest ≤ 24 h |
| petss | P-ETSS storm surge text (e90→p10, e10→p90) | ft surge hourly | 102 h | warm job | cycle age ok ≤ 12 h, degraded ≤ 30 h, else unavailable; every point needs a parseable stamp and a finite plausible value; p10 ≤ p90 guard per hour |
| nbm | NBM core 6-h QPF + PoP (NOMADS subset) | in, % | to now+168 h (fh ≤ 264) | warm job | cycle age ok ≤ 9 h, degraded ≤ 24 h; a bucket needs parseable end, finite hours in (0, 48], finite QPF ≥ 0, PoP in [0, 100]; malformed rows dropped, counted, and the source reads degraded |
| nbm_qmd | NBM probabilistic: 6-h p10/p50/p90, P(≥0.25/0.5/1 in) | in, % | as nbm | warm job, newest synoptic cycle | cycle age ok ≤ 15 h, degraded ≤ 30 h; expiry decided before merge; fields cleared before merge; a bucket with non-finite, negative or disordered percentiles is skipped |
| wpc | WPC 24-h QPF days 2–7 | in | 168 h | warm job | cycle age ok ≤ 18 h, degraded ≤ 36 h; same bucket shape rules as nbm |
| xcheck | non-NOAA multi-model + GEFS ensemble | in, mph | 7 d | hourly run | display cross-check only; never an input |
| surge_mean (v0.10.6, PRODUCTION) | trailing 364-d mean of verified CO-OPS `hourly_height` minus hourly predictions (`forecast/surge_mean.py`) | ft | window ends ~3 weeks back (verified lag) | warm job, at most daily, `data/surge_mean.json` | >= 300 days of paired hours, mean in (-2, 3) ft; ok <= 7 d old, degraded <= 45 d, else the documented fallback constant +0.54 ft (trailing mean to 2026-09-22), labeled |
| surge state (v0.10.6, PRODUCTION) | last FRESH observed surge + its time, `data/surge_state.json` | ft, UTC | — | written by the hourly run's main() only | parseable, finite, not in the future; used only by the stale-state rung, never older than 168 h |

Hourly-run acquisition: five quick sources (seven HTTP requests: the grid
and the cross-check each make two) under one 60-s monotonic deadline, 15 s
cap per request, with the timeout recomputed from the deadline before EVERY
request, nested ones included; a spent budget raises before the next request.
The quick fetches run in a worker thread that the run waits on for at most a
**75-s wall clock** (budget + one cap); anything unfinished is reported
"unavailable: wall-clock budget" and abandoned. So the alert path waits at
most 75 s on the optional outlook acquisition (the workflow's 5-minute
timeout is the outer bound); it never waits on NOMADS, which the warm job
(`data/outlook_guidance.json`, every 3 h) owns.

### Precedence

- **Outlook hourly surge (v0.10.6, ONE estimator for the chart line, the
  extended maps and the per-tide table: `outlook.hourly_surge_estimator`):**
  (1) NWPS hourly (<= 72 h) with the advisory correction: at each advisory
  (CFW) tide, correction = advisory total minus the larger bracketing NWPS
  hourly value; corrections interpolate between advisory times, are held
  1 h outside the first/last and fade to zero over a further 6 h (source
  `nws_product` where a correction applies, else `nwps`); (2) P-ETSS hourly
  surge, midpoint of p10/p90 (<= 102 h), for hours NWPS does not cover
  (`petss_hourly`); (3) beyond guidance, the LAST guidance hour's surge
  decaying toward the typical offset with tau 36 h (`guidance_decay`; an
  ASSUMPTION: the decay law was measured on observed readings); (4) with no
  guidance at all, the observed reading decaying from its own time, identical
  to the production curve (`observed_decay`); (5) the typical offset
  (`typical_offset`). Each hour carries its source; the chart draws every
  source switch as a labeled dashed boundary; nothing is smoothed across one.
- **Outlook per high tide (cards, table, shadow ledger):** nws_product -> nwps
  (<= 72 h, shadow) -> petss midpoint (<= 102 h, band shown) -> guidance_decay
  (the estimator's value at that tide) -> persist_decay (observed reading,
  toward the trailing mean, tau 36 h; still logged for shadow scoring) ->
  astronomy labeled "no surge guidance".
- **Rain per hour:** NWS grid inside its reach → NBM 6-h uniform rate → WPC
  24-h uniform rate → unknown (flagged, never zero). Buckets are [start, end).
  Day totals are prorated by overlap with the station-local day.
- **Production surge (v0.10.6, `forecast/surge_decay.py`):**
  surge(t) = mean + (s_obs - mean) * exp(-(t - t_obs) / 36 h), from the
  READING's time. Ladder, newest valid reading first: `fresh` (<= 60 min)
  -> `stale-download` (older reading still in the 6-h observation download)
  -> `stale-state` (last fresh reading saved by an earlier run) ->
  `typical-offset` (surge = mean exactly), snapping to typical-offset when a
  non-fresh reading's decayed departure is < 0.05 ft or it is > 168 h old.
  Labels: "surge +x ft from a reading N h ago, decaying toward the typical
  offset +m ft (time constant 36 h)" or "surge unavailable, using the
  typical offset (+m ft, source)".
- **Production per high tide:** product row within 2 h, else the decayed
  surge at that tide (`surge-persistence`, fresh or stale), else astronomy +
  the typical offset (`typical-offset-degraded`; was zero-surge
  `astronomical-only-degraded`, which remains valid in historical rows).
- **Production continuous curve:** each 30-min point gets the decayed surge
  at its own time; the tank runs on that curve. Never the worst product
  tide's surge. A missing reading no longer removes the curve: the ladder
  always yields a labeled value (v0.10.5's interim no-curve rule is
  retired, owner decision `missing-surge-ladder`). The nowcast's explicit
  0.0 astronomy-only caller is unchanged. `water_series_input` records
  source (`surge-persistence` | `typical-offset`), the surge now, the
  reading's time and age, status (ok only for a fresh reading), the label,
  and a `decay` object (rung, reading, mean, mean source, tau).
- **Scoped health:** `input_health` retains every source; `degraded_inputs`
  lists only production failures and `outlook_degraded_inputs` only outlook
  failures. Outlook pages/maps display their own failures; core email/widget
  do not warn about unused outlook inputs. Maps label the source transition.

### Horizon and scope
- Outlook: a ROLLING 168 h from the run instant. The hourly series runs from
  −6 h to +168 h; the tide table lists every high tide ≤ 168 h; the slider
  reaches the last series point. Day cards cover every station-local calendar
  date the horizon touches (eight in general); the first and last are marked
  partial with their hours in scope, and a partial last card may hold no high
  tide. `horizon_hours` (168) is the requested horizon; the final hourly
  sample can be less than one hour short of that boundary.
- Unknown rain: an hour with no rain forecast is flagged `rain_unknown` in the
  series, `u` in the map payload, gray on the chart, and labeled "tide-only,
  not a flood forecast" at that instant on both maps. Day-card rain coverage
  counts only VALID intervals: explicit null intervals are unknown hours
  (`qpf_null_h`); day-total aggregation assumes absent intervals inside the
  NWS grid's reach are dry. Hourly rain lookup still tries NBM/WPC for missing
  values. `qpf_partial` marks coverage short of that local day's actual length
  by more than 0.5 h (including 23/25-hour DST days).
- Alerts (ntfy/email): tides ≤ 48 h ahead only (`ALERT_WINDOW_HOURS`); SMS is
  nowcast-only. Display horizons (72-h rollup, 7-day outlook) never widen
  alerting.

### Rain scenarios
- Tank line: the production tank integrated on the tide+surge curve.
- Burst scenario: analog 1.7 × (max 6-h / 0.55) capped 3.0 in/hr when
  burst-capable hours or convective wording exist; the max 6-h amount is
  taken over timestamped 6-h windows and flagged when the window is
  incomplete. Three NAMED scenarios: (i) rain-alone at the fixed 2.5 ft
  NAVD88 low bay (cards); (ii) time-local compound, the burst at each
  burst-capable hour's own tide level (chart band, both maps' per-point
  potential, and the "worst flood chance" selection); (iii) hypothetical
  burst on the day's high tide (cards only; it has no clock, is not drawn
  on the chart and is never the timed worst selection).
- NBM 90th-percentile scenario: the same analog on the p90 6-h amount, shown
  as the rain band's labeled high end with its exceedance chance; it does not
  drive the headline (mirrors the P-ETSS high end).
- Summed 6-h percentiles are not daily percentiles and are named as sums.

### Day-scoped worst pathway (headline rule, AGENTS rule 6)
`day_worst` = max over {tide regime of the day's tides; regime of the day's
highest continuous water; burst-potential regime on burst-capable hours;
categorical rain watch at rank 1}, computed from the run instant FORWARD
(tides and series points at or after `generated_utc`; past water stays in
the labeled lookback). Landing cards, ribbon, today's headline, and the
email subject, text and HTML "WORST 72 H" panel all read the same result
through one function. Widget keeps its owner-set tidal-days / rain-days
lines. The former residual (the 30-h series carrying the worst product
tide's surge on every hour) was removed by the 2026-09-23 recovery
(`eb8e79475`): the curve uses observed-surge persistence.

### Shadow scoring and promotion policy
`data/outlook_log.csv` (append-only) logs every source per tide per run.
Scoring unit: one issued forecast of one tide. Within each lead bucket,
absolute errors are averaged within a tide first, then across tides with
equal weight (MAE); signed errors the same way, separately (bias).
Candidate and baseline are paired only on the same ledger row (same
issuance and target); `n` = distinct tides, `n_obs` = tide-bucket pairs,
`n_forecasts` = paired rows. READY requires 28 distinct tides and a lower
MAE; promotion of any shadow source into production remains human-gated
(rule 12).

### Surfaces that show the curve against the advisory (v0.10.6)
The landing chart and the 7-day chart plot the NWS advisory's own high-tide
numbers as red diamonds. The production curve is observed-surge decay and
does not pass through them; any gap is shown, not blended away.

### Confidence labels
Retired from every human surface and from `forecast.json`; ledgers keep the
column and write it empty; the measured error by lead replaces them, named
as Sandy Hook tide-peak gauge skill.

## Landmark ladder

Eighteen surveyed landmarks, ascending — the v0.10.1 ladder; the
nineteenth entry added in v0.10.2 was removed in v0.10.4: grate_SW 3.52 · grate_SE 3.60 · corner_SE 3.64 ·
corner_SW 3.64 · grate_bay_ave_upstream 3.64 · gutter_walkway 3.78 ·
grate_NE 3.80 · grate_NW 3.80 · corner_NE 3.91 · corner_NW 3.91 · curb
4.16 · sidewalk_under_walkway_lawn_step 4.33 · road_middle 4.36 ·
intersection_highpoint 4.54 · lawn_step 4.66 · porch_step_base 4.68 ·
porch_step1_top 5.41 · porch_deck 8.08 (ft NAVD88; Sandy Hook MLLW =
NAVD88 + 2.82).

## Dynamic pluvial tank

```text
dV/dt  = TANK_K · max(0, R(t − lag) − D(bay))^TANK_GAMMA
          − TANK_KOUT · V
stage  = corrected_stage_storage_inverse(base_stage, +V)
water  = 3.52 + stage/12          (NAVD88)

TANK_K       = 1.296e6 cell-in/h at 1 in/hr net
TANK_GAMMA   = 0.78
TANK_KOUT    = 3.50 /h            (~17-minute reservoir e-folding)
TANK_LAG_MIN = 15 min
D(bay)       = head-dependent drain (0.25 in/hr below bay 3.0,
               tapering to 0 at bay 3.52)
```

Unchanged from v0.10.3 (which corrected only the sub-bin stage-storage
inverse; constants from the v0.10.1 refit). Forecasts use raw hourly NWS
QPF; nowcasts use observed MRMS PrecipRate over the catchment. Validated
tank arithmetic is not validated rain-input skill. The nowcast still
holds one bay head through its 45-minute projection; the moving-head
candidate remains HELD (see `archive/v0.10.3.md`).

## Tide-keyed pathway

For each high tide:

```text
SH peak MLLW = matching NWS product TOTAL, when available
            = astronomical tide + decayed surge at the tide's time, otherwise
              decayed surge = mean + (s_obs − mean) × exp(−(t_tide − t_obs) / 36 h)
              (fresh, stale-download or stale-state reading; source surge-persistence)
            = astronomical tide + mean (the typical offset), with no usable reading
              (source typical-offset-degraded)
water NAVD88 = SH peak MLLW + 0.00 − 2.82
depth at landmark = max(0, water − landmark NAVD88) × 12 inches
```

`astronomical-only-degraded` (zero surge) is no longer produced; it remains
valid in historical rows written before v0.10.6. Offshore peak winds retain the separately
reported −0.13-ft expected-actual adjustment. The retired cold lockout
remains advisory only.

## Static scenarios

The map slider, rain calculator, and advisory scenarios use the same tank
constants at steady state and the corrected stage-storage inverse:

```text
V_steady = (TANK_K / TANK_KOUT) · net_rate^TANK_GAMMA
```

The saturating tanh estimate remains a labeled conservative comparison.
The v0.9 power law remains only as a legacy comparison; neither drives
the primary hydrograph.

## Calibration evidence and limitations

- Measured peak anchors: six — December 19 and October 30, 2025; July 6,
  July 9, July 18, and August 3, 2026.
- Full fit hydrographs: two — July 6 and July 9, 2026.
- Independent peak checks: October 30 and December 19, 2025.
- Independent measured recession constraint: one — July 18, 2026.
- Out-of-sample hindcast: August 3, 2026.
- Post-cutover measured Events 7–9 remain recipe hindcasts, not additions
  to the frozen calibration set.
- Reproduction: the numeric production replay is unchanged —
  [`history/scripts/reproduce_v0_10_3.py`](../history/scripts/reproduce_v0_10_3.py)
  against [`model/data/v0.10.3-reproduction.json`](data/v0.10.3-reproduction.json)
  (its version check accepts a documented successor whose archived spec
  exists); the legacy v0.10.1 record replays via
  [`history/scripts/reproduce_v0_10_1.py`](../history/scripts/reproduce_v0_10_1.py).

Known structural gaps remain deliberately untuned: event-dependent lag,
antecedent wetting, bidirectional drainage and hysteresis, a tilted
recession pool, recirculation between grates, long hillside delivery, and
the sidewalk swale micro-basin. Future changes must be evaluated one
mechanism at a time and versioned explicitly, with independent candidate
review and a recorded owner decision before promotion (AGENTS rule 12).
